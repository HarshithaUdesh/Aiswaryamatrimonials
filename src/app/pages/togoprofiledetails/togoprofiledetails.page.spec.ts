import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TogoprofiledetailsPage } from './togoprofiledetails.page';

describe('TogoprofiledetailsPage', () => {
  let component: TogoprofiledetailsPage;
  let fixture: ComponentFixture<TogoprofiledetailsPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TogoprofiledetailsPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
